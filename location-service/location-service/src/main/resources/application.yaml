server:
  port: 8082

spring:
  application:
    name: location-service

  data:
    redis:
      host: localhost
      port: 6379
#      password: password
#      database: 0

#Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info
